import FluidObject from "./FluidObject";

/*
*   it's a class to describe bullet
*
*/
export  default class Bullet extends FluidObject{
    // something to be  achieved ...
    // just like the type of bullet
    // because rifle and pistol may have different bullets to be shown in front-end
}